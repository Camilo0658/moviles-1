import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.techstore.app',
  appName: 'TechStore',
  // Carpeta que genera "next build" con output: 'export'
  webDir: 'out',
};

export default config;
