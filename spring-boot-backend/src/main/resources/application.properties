# Configuración del servidor
# Railway inyecta el puerto en la variable PORT. En local usa 8080.
server.port=${PORT:8080}

# Configuración de MongoDB Atlas
# La URI se toma de la variable de entorno MONGODB_URI (Railway / Atlas).
# IMPORTANTE: la URI debe incluir el nombre de la base de datos (techstore) antes del "?".
spring.data.mongodb.uri=${MONGODB_URI:mongodb+srv://camiloovalle0658_db_user:WAXOLiFTEHpUrPtq@cluster0.14gtyiq.mongodb.net/techstore?retryWrites=true&w=majority&appName=Cluster0}

# Nombre de la base de datos (refuerzo por si la URI no lo trae)
spring.data.mongodb.database=techstore

# Configuración de CORS (para permitir peticiones desde el frontend)
# Se configura en la clase WebConfig.java
